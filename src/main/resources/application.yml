---
server:
  port: 8083

spring:
#  liquibase:
#    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/task-tracker
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
     hibernate:
       ddl-auto: update
     show-sql: true